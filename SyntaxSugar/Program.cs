//this is a syntax sugar code. Where the code is not required nor necessarily best practice, but is makes coding convient & concise.

var answer = 4;

var response = (answer > 0) ? $"{answer} is less than nine" : $"{answer} is greater than or equal to nine";

Console.WriteLine(response);
